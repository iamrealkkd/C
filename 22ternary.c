#include<stdio.h>
#include<conio.h>
int main(){
    int a=7;
    int b=25;
    int result = a > b ? 100 : 200; // Store the result
    printf("The result is: %d\n", result); // Print the result
    return 0;
}
